# CONTEXT-MAP.md

> 单 CONTEXT 仓库占位说明。多端拆分前**不要**建分端 CONTEXT。

| Context | 路径 | 状态 |
|---|---|---|
| 默认（全仓） | [`CONTEXT.md`](./CONTEXT.md) | active |

若未来拆 `admin` / `api` / `weapp`：

1. 在本文件登记各端路径（如 `docs/contexts/api/CONTEXT.md`）
2. 系统级决策仍放 `docs/adr/`
3. 更新 `docs/agents/domain.md` 为 multi-context 消费规则
