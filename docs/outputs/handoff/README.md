# handoff/

任务交接快照（覆盖式更新：同 theme 只留最新一份）。

## 约定

```text
docs/outputs/handoff/{theme}/YYYY-MM-DD-{branch}-{task}.md
```

模板与场景见 `docs/agents/handoff.md`（默认场景 A）。

## 占位

尚无实施中的 theme。开写第一个 handoff 时在本目录下建 `{theme}/`。
